# Public (exposed to browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_SITE_NAME="ZeroDay Security AI"

# Server-side only (never prefix with NEXT_PUBLIC_)
# Add server-only secrets here as they're introduced in later phases.

# AI Assistant -- set to a valid access token for the live chat UI to work
NEXT_PUBLIC_DEMO_TOKEN=
